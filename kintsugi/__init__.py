"""Kintsugi — a self-healing Jam-generation runtime.

prompt -> plan -> generate -> run/validate -> repair -> publish, with a measured
eval loop that gates it. See README.md.
"""

__version__ = "0.1.0"
